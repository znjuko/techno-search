#include "graph_converter.h"
#include "shop_map.h"
#include <vector>


std::vector<vector<double>> GraphConverter::Generate(Map map) {

    std::vector<Feature> counters = map.getCounters();
    Feature shop = map.getShop();


}